{
  "name": "amdesigns-inventory",
  "version": "2.1.0",
  "description": "AMDesigns inventory dashboard — Vercel static + KV serverless sync",
  "scripts": {
    "dev": "npx serve ."
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

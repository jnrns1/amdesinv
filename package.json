{
  "name": "amdesigns-inventory-webhook",
  "version": "1.0.0",
  "description": "Webhook server for AMDesigns inventory sync (Shopify + Excel)",
  "main": "webhook.js",
  "scripts": {
    "start": "node webhook.js",
    "dev": "node webhook.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
